# To make controller work on ubuntu

sudo add-apt-repository ppa:rael-gc/ubuntu-xboxdrv
sudo apt update
sudo apt install xboxdrv

rmmod xpad # maybe unnecessary

sudo apt remove ubuntu-xboxdrv

sudo xboxdrv -c keys.xboxdrv   # file attached
